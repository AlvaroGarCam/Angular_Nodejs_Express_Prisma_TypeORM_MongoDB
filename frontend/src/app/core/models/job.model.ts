// import { Profile } from "./profile.model";

export interface Job {
    slug: string;
    name: string;
    description: string;
    salary: number;
    company: string;
    images: [],
    img: string,
    id_cat: string;
    // favorited: boolean;
    // favoritesCount: number;
    // author: Profile;
}

